# COLOSSUS

# Colossus Maps (MVP)

Industrial site visualizer for TX, CA, AZ. Map on top, cards below, local bookmarks.

## Run
Open `index.html` in Chrome. Or serve statically:
```sh
python -m http.server 5173
# then open http://localhost:5173
